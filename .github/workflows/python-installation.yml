name: Installing Python

on: [push]

jobs:
  strategy:
    matrix:
      os: [ubuntu-latest, windows-latest]
  build:
    runs-on: ${{ matrix.os }}
    steps:
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: 3.10
        
